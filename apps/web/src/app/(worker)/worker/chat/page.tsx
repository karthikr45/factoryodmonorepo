/**
 * Worker chat is the same UI as factory chat — same /chat/* endpoints,
 * same ChatRoom membership model. We re-export to keep one source of truth.
 */
export { default } from '../../../(factory)/factory/chat/page';
