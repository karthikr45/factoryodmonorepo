{
  "name": "@factoryos/ai",
  "version": "0.0.0",
  "private": true,
  "description": "FactoryOS Python AI service (OCR, insights, assistant)",
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "lint": "echo \"python lint handled via ruff in CI\"",
    "typecheck": "echo \"python typecheck handled via mypy in CI\"",
    "test": "pytest",
    "clean": "rm -rf __pycache__ .pytest_cache"
  }
}
